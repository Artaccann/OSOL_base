// Pozadí pro Chapter 1
const bg_room = new Image();
bg_room.src = '../Sprites/room.png';

const bg_oldroom = new Image();
bg_oldroom.src = '../Sprites/oldroom.png';

const bg_outside = new Image();
bg_outside.src = '../Sprites/outside.png';

const CH1_BACKGROUNDS = {
  room: bg_room,
  outside: bg_outside,
  oldroom: bg_oldroom,
};
